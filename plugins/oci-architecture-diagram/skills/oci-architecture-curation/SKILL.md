---
name: oci-architecture-curation
description: Close the integrated OCI architecture and BoM case memory while promoting only sanitized, evidence-backed lessons.
---

# OCI Architecture Curation

Audit case memory with node scripts/case-memory.mjs audit <workspace> <case-slug>. Keep customer evidence, names, costs, identifiers and quotes in the case directory. Shared knowledge may contain only anonymized reusable patterns with evidence and limits.

Record final handoff statuses and whether the exact BoM JSON received clean browser import validation. Do not promote a lesson merely because a case completed.
